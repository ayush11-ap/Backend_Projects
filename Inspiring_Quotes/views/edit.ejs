<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-gray-200">
    <div class="main h-screen flex items-center justify-center">
      <div
        class="bg-gray-800 border border-gray-700 rounded-lg px-8 py-6 flex flex-col gap-6 shadow-lg w-full max-w-lg"
      >
        <h2 class="text-center text-3xl font-semibold text-gray-100">
          Edit Your Post
        </h2>
        <p class="text-gray-400">
          Post ID:
          <span class="font-medium text-gray-300"><%= post.id %></span>
        </p>
        <p class="text-gray-400">
          Username:
          <span class="font-medium text-gray-300">@<%= post.username %></span>
        </p>
        <form
          class="flex flex-col gap-4"
          method="post"
          action="/posts/<%=post.id%>?_method=PATCH"
        >
          <textarea
            class="border border-gray-600 rounded-lg bg-gray-700 text-gray-200 px-4 py-3 font-serif shadow-inner focus:border-blue-500 focus:outline-none"
            rows="10"
            cols="5"
            name="content"
          >
<%= post.content %></textarea
          >
          <button
            type="submit"
            class="mt-4 px-4 py-2 bg-blue-600 text-gray-100 font-semibold rounded-lg hover:bg-blue-500 transition-all duration-200 shadow-lg"
          >
            Submit
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
