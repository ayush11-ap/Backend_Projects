<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Inspiring Quotes</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900">
    <h1
      class="flex items-center text-gray-200 justify-center lg:py-6 lg:text-4xl font-semibold"
    >
      Inspiring Quotes
    </h1>
    <div
      class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 px-6 lg:px-20 mt-10 lg:mt-4"
    >
      <% for(post of posts) { %>
      <div
        class="bg-gray-800 p-6 rounded-lg shadow-lg flex flex-col h-full border border-gray-700"
      >
        <h3 class="text-gray-300 font-semibold text-lg">
          @<%= post.username %>
        </h3>
        <p class="mt-4 text-gray-400 text-lg flex-grow"><%= post.content %></p>
        <div class="w-full flex justify-center mt-6 space-x-4">
          <a
            href="http://localhost:8080/posts/<%=post.id%>"
            class="px-4 py-2 bg-gray-700 text-gray-300 rounded hover:bg-gray-600 transition-colors"
          >
            Details
          </a>
          <a
            href="http://localhost:8080/posts/<%=post.id%>/edit"
            class="px-4 py-2 bg-gray-700 text-gray-300 rounded hover:bg-gray-600 transition-colors"
          >
            Edit
          </a>
          <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
            <button
              class="px-4 py-2 bg-red-600 text-gray-200 rounded hover:bg-red-700 transition-colors"
            >
              Delete
            </button>
          </form>
        </div>
      </div>
      <% } %>
    </div>

    <br />
    <br />
    <div class="flex justify-center">
      <a
        href="http://localhost:8080/posts/newPost"
        class="px-4 py-2 bg-sky-600 text-gray-200 rounded font-semibold hover:text-zinc-300"
      >
        Create New Post
      </a>
    </div>
  </body>
</html>
